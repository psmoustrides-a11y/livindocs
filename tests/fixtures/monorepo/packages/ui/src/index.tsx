import React from 'react';
import ReactDOM from 'react-dom/client';
import { formatResponse } from '@monorepo/shared';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(<App />);
